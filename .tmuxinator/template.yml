name: hacking
root: ~/dev

# Runs on project start, always
on_project_start: "powerline-config tmux setup"

windows:
  - repo:
      root: "~/dev/repo"
      layout: even-vertical
      panes:
        - code .
        -
  - tmp:
      root: "~/tmp"
      layout: even-vertical
      panes:
        - ls
        -
